TREELIB {
    BST {
        ARGV, # function argument value
        EMPTY, # tree is empty
        ICMP, # invalid compare function
        DNF, # data not found
        DUP, # duplicate node
        ITOD # invalid traverse order
    }
    RBT {
        ARGV, # function argument value
        ICMP, # invalid compare function
        DUP, # duplicate node
        EMPTY, # tree is empty
        DNF, # data not found
        ITOD # invalid traverse order
    }
    BT {
        ARGV, # function argument value
        OBD # out of bound
    }
}